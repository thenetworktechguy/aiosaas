# litefs.yml
fuse:
  dir: "/var/lib/litefs"

data:
  dir: "/var/lib/litefs"

exec:
  - cmd: pocket-react serve --http=0.0.0.0:8090 --dir=/litefs/data/

proxy:
  addr: ":8090"
  target: "localhost:8091"
  db: "/var/lib/litefs/data/data.db"
